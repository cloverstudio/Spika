<div class="message-cell {{isMine}} text" id="{{id}}" userid="{{userID}}">
    
    <div class="status status{{status}}">
        
        <span class="sending glyphicon glyphicon-arrow-right"></span>
        <span class="sent glyphicon glyphicon-ok-circle  "></span>
        
    </div>
        
	<div class="avatar">
		<img src="{{ avatarURL }}" />
    </div>

    <div class="info">
        
        <span class="name"> {{name}} </span>
        <span class="time"> {{formatTime created}} </span>        
        
    </div>
    
    <div class="message">
        <div>
            {{l10n "Name"}} : {{vcard.name}} <br />
            {{#if vcard.telhome}} {{l10n "Phone"}} : {{vcard.telhome}} <br /> {{/if}}
            {{#if vcard.telcell}} {{l10n "Cell"}} : {{vcard.telcell}} <br /> {{/if}}
            
         </div>
    </div>
    
    <div class="clearfix"></div>
    
</div>

